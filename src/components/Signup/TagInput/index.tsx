// Re-export from the shared UI component.
// Prefer importing directly from '@/components/ui/tag-input' in new code.
export {
  TagInput,
  type TagInputProps,
  type TagOption,
  toTagOptions,
} from '@/components/ui/tag-input'
